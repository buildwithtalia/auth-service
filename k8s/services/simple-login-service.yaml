apiVersion: v1
kind: Service
metadata:
  name: simple-login-service
  namespace: auth-system
  labels:
    app: simple-login-service
spec:
  selector:
    app: simple-login-service
  ports:
  - port: 3001
    targetPort: 3001
    name: http
  type: ClusterIP